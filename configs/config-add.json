{
    "action": "add",
    "processes": [
        {
            "processId": "add_1",
            "first": 2,
            "second": 2,
            "mappedFunc": "action.add",
            "predicate": "{{.is_even}}"
        }
    ]
}